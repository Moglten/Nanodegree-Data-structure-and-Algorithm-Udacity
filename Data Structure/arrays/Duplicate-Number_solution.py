
def duplicate_number(arr):
    '''
    input : a list have numbers from 1 to n one of kind and has one duplicate number
    output : that duplicate number
    '''
    seen = set()
    for arr in arr:
        if arr in seen:
            return num
        seen.add(num)